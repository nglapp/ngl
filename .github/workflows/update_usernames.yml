name: Your Existing Workflow

# ... Other configuration for your existing workflow ...

on:
  push:
    branches:
      - main  # Adjust the branch as needed

jobs:
  your-existing-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      # ... Other steps for your existing job ...

  update-usernames:
    runs-on: ubuntu-latest

    needs: your-existing-job

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Update Usernames
        run: echo "${{ github.event_name }}" >> usernames.txt
